name: PR Labeler
on:
  pull_request_target:
    types: [opened, edited]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      # bcoe/conventional-release-labels@v1.3.1
      - uses: bcoe/conventional-release-labels@886f696738527c7be444262c327c89436dfb95a8
        with:
          type_labels: |
            {
              "feat": "enhancement",
              "fix": "bug",
              "docs": "documentation",
              "ci": "ci",
              "chore": "chore"
            }
          ignored_types: "[]"
